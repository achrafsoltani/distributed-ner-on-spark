# Distributed NER on Spark — Python dependencies
#
# Install into a project venv:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# LLM teacher (via AWS Bedrock)
anthropic>=0.40.0
boto3>=1.35.0

# Data wrangling
pandas>=2.2.0
pyarrow>=17.0.0
numpy>=1.26.0
duckdb>=1.0.0

# Student training & evaluation
spacy>=3.7.0
spacy-lookups-data>=1.0.0
scikit-learn>=1.4.0
transformers>=4.44.0
optimum[onnxruntime]>=1.23.0
huggingface_hub>=0.25.0

# Plotting (Fig. 7 long-tail scatter)
matplotlib>=3.8.0

# Configuration
python-dotenv>=1.0.0

# Dev quality-of-life
tqdm>=4.66.0
